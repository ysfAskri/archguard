# archguardian sbom

Generate SBOM. Run `npx archguardian sbom --format cyclonedx` (or `spdx`). Collects dependencies from package.json, go.mod, Cargo.toml, pom.xml. Outputs CycloneDX 1.5 or SPDX 2.3 with PURLs.
