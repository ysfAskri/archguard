# archguardian dead-code

Detect unused exports. Run `npx archguardian scan --format json` with dead code analyzer enabled. Filter `dead-code/unused-export` findings. Review whether exports are truly unused or used outside the analyzed scope. Configure `entryPoints` for legitimate entry files.
