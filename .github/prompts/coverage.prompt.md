# archguardian coverage

Integrate test coverage. Run `npx archguardian scan --format json` with coverage analyzer enabled. Reads lcov or Istanbul JSON reports. Flags files below minCoverage threshold (default 80%) and uncovered new code below minNewCodeCoverage (default 90%).
