Generate architecture diagram of project dependency structure.

1. Run `npx archguardian diagram --format mermaid` with optional `--scope <glob>`
2. Display the Mermaid diagram showing directory subgraphs and dependencies
3. Suggest scope filtering for large projects
