# archguardian licenses

Check dependency license compliance. Run `npx archguardian scan --format json` with `analyzers.licenses.enabled: true`. Configure `allowed` and `denied` license lists with glob patterns. Fetches from npm registry and crates.io.
