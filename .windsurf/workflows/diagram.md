# Architecture diagram

Generate architecture diagram of project dependency structure.

## Steps

1. Run: `npx archguardian diagram --format mermaid` with optional `--scope <glob>`
2. Display Mermaid diagram with directory subgraphs
3. Explain the architecture layout
4. Suggest scope filtering for large projects
